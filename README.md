# homebrew-csv-validator

Install the [csv-valifator](http://digital-preservation.github.io/csv-validator/) via homebrew.

## Install
```bash
brew tap ringohub/csv-validator
brew update && brew doctor
brew install csv-validator
```
